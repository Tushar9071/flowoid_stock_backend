# common

This folder is for shared, cross-module code that doesn't belong to a single feature module.

Examples:
- shared types
- shared validators
- pagination helpers
- base service utilities

Keep `utils/` for truly generic helpers; put domain-shared code here.
